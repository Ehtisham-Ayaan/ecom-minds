<%= form_with(model: savermart) do |form| %>
  <% if savermart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(savermart.errors.count, "error") %> prohibited this savermart from being saved:</h2>

      <ul>
        <% savermart.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
